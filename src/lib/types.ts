export interface Movie {
  slug: string
  img: string
  title: string
  description: string
  genres: string[]
  trailer: string
  released: string
  ends: string // date that comes out of cinema
  director: string
  cast: string[]
  price: number
}
